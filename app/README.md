# Solo Operator Cockpit — App

See the root [README](../README.md) for full setup, deployment, and usage docs.

## Quick start

```bash
cp .env.example .env   # fill in your API keys
npm install
npm run dev            # http://localhost:5173
```

Windows: double-click `run.bat`.
